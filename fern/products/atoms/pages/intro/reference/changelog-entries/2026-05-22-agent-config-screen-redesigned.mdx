---
title: Agent config screen — redesigned layout
date: 2026-05-22
description: The agent configuration screen has been redesigned for clarity and faster editing — grouped panels, inline help, and validation closer to where you type.
---

The agent configuration screen in the Atoms console has been redesigned. Same fields, same data model — clearer layout, faster editing, inline validation.

**What's new in the UI:**

- Configuration is grouped into focused panels (Identity, Voice, LLM, Tools, Webhooks, Compliance) instead of one long form.
- Inline validation surfaces errors as you type instead of after submit.
- Help text and links to the relevant docs page live next to each field.
- Mobile / narrow-viewport layout is now usable end-to-end.

**Migration:** no action — existing agents are unchanged. Open any agent in the console to see the new layout.
