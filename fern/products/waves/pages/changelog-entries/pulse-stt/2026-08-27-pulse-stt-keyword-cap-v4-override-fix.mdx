---
title: "Pulse STT keyword boosting: v4 API-reference render now shows 100 (docs fix)"
---

Follow-up to yesterday's correction. The [previous PR](/models/documentation/speech-to-text-pulse/features/word-boosting) fixed the base spec and the feature page, but the v4 API-reference override (which is the source that actually renders under **API Reference → Speech to Text → Transcribe (Realtime / WebSocket)**) was missed. That override still advertised 10,000. It now reads 100, matching the runtime limit and the rest of the docs.
