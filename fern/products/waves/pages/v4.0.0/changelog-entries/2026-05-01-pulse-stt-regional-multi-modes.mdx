---
title: Pulse STT — `multi-indic` and `multi-asian` regional auto-detection modes
---

The `language` parameter on Pulse STT (both pre-recorded REST and realtime WebSocket) now accepts two new regional auto-detection scopes alongside the existing `multi-eu` and `multi`:

- **`multi-indic`** — auto-detects across the Indic set: `en`, `hi`, `mr`, `pa`, `gu`, `or`, `ka`, `ta`, `te`, `ml`, `bn`. Use for Indian-language audio with optional English code-switching.
- **`multi-asian`** — auto-detects across the East Asian set: `en`, `ja`, `ko`, `zh`, `yue`. Use for Japanese / Korean / Mandarin / Cantonese audio with optional English code-switching.

The existing scopes are unchanged:
- `multi-eu` (default) — `de`, `en`, `fr`, `it`, `nl`, `pt`, `ru`, `es`.
- `multi` — full multilingual auto-detection across all supported languages.

**Pick the narrowest scope that matches your audio** for the best accuracy. Omitting `language` still routes to `multi-eu` and can mis-detect on non-European audio (e.g., returning Russian for English input).

Specs updated:

- AsyncAPI: `fern/apis/waves/asyncapi/pulse-stt-ws.yaml` and `pulse-stt-ws-overrides.yml`
- OpenAPI (pre-recorded): `fern/apis/waves/openapi/pulse-stt-openapi.yaml`
- v4 override: `fern/apis/waves-v4/overrides/pulse-stt-ws-overrides.yml`
- Reference page: `fern/products/waves/pages/v4.0.0/api-references/pulse-stt-ws.mdx`

Probe baseline (`scripts/probes/baseline-pulse-stt.json`) extended with two new test cases (`lang-multi-indic`, `lang-multi-asian`) so the weekly probe verifies these modes stay live.

→ [Pulse STT WebSocket reference](/waves/api-reference/api-reference/speech-to-text/speech-to-text)
→ [Pulse STT pre-recorded](/waves/documentation/speech-to-text-pulse/pre-recorded/quickstart)
