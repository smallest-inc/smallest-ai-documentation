# Overrides for voice-catalog-openapi.yaml.
# Tags the endpoint with the v4docs audience so products/waves.yml can render it.

paths:
  /waves/v1/voice/get-all-models:
    get:
      x-fern-server-name: waves
      x-fern-audiences: [v4docs]
      tags: [Text to Speech]
