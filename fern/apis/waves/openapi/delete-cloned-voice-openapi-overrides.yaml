# Override file for delete-cloned-voice-openapi.yaml
# Specifies waves server for all endpoints

paths:
  /api/v1/lightning-large:
    delete:
      x-fern-server-name: waves
      description: |
        Delete a cloned voice using the new Waves API.
