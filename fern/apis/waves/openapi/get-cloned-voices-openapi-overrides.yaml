paths:
  /waves/v1/lightning-large/get_cloned_voices:
    get:
      operationId: get_cloned_voices
      deprecated: true
      x-fern-availability: deprecated
      x-fern-sdk-method-name: get_cloned_voices
      x-fern-server-name: waves
      x-fern-audiences:
      - v2
      - v3
      - v4
      summary: Get your cloned Voices (Deprecated)
      description: '**Deprecated** — use `GET /waves/v1/voice-cloning` instead. The new

        list endpoint returns the same data plus a `modelIds` array per

        clone. Kept live for backward compatibility.

        '
