paths:
  /waves/v1/lightning-large/get_cloned_voices:
    get:
      operationId: get_cloned_voices
      deprecated: true
      x-fern-sdk-method-name: get_cloned_voices
      x-fern-server-name: waves
      x-fern-audiences:
        - v2
        - v3
        - v4
      summary: Get your cloned Voices (Deprecated)
      description: |
        **Deprecated** — use `GET /waves/v1/voice-cloning` instead. That
        endpoint returns the same data plus a `modelIds` array indicating
        which models each clone is compatible with.

        Kept live for backward compatibility. Returns all voice clones
        on the authenticated organization regardless of model.
