# SDK gen overrides for Hydra Speech-to-Speech WebSocket
# Paired with ../waves/asyncapi/hydra-ws.yaml via fern/apis/unified/generators.yml

channels:
  hydraSession:
    x-fern-sdk-group-name: Speech to Speech
