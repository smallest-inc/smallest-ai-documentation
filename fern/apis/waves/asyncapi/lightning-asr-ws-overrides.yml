# Override file for lightning-asr-ws.yaml
# Adds unique type names to prevent collisions with other AsyncAPI files
# Maps server reference to match generators.yml environment URLs

servers:
  waves:
    host: waves-api.smallest.ai
    pathname: /api/v1/lightning/get_text
    protocol: wss

channels:
  /api/v1/lightning/get_text:
    x-fern-sdk-group-name: Speech to Text Streaming
    servers:
      - $ref: "#/servers/waves"
    messages:
      audioData.message:
        payload:
          title: LightningAudioData
      endSignal.message:
        payload:
          title: LightningEndSignal
      transcriptionResponse.message:
        payload:
          title: LightningTranscriptionResponse
