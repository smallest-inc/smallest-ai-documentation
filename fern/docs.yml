# yaml-language-server: $schema=https://schema.buildwithfern.dev/docs-yml.json

instances:
  - url: smallest-ai.docs.buildwithfern.com
    custom-domain: docs.smallest.ai

integrations:
  context7: ./context7.json

title: Smallest AI Docs

ai-search:
  model: claude-4
  location:
    - docs
  system-prompt: |
    You are the Smallest AI documentation assistant for developers
    integrating speech AI.

    ## Products and exact model names
    - Pulse — current speech-to-text (STT) model. 39 languages. Two
      modes: streaming (WebSocket) and pre-recorded (HTTP POST file
      upload). Features: speaker diarization, PII/PCI redaction, ITN,
      keyword boosting, word/sentence timestamps, code-switching.
    - Lightning v3.1 — current text-to-speech (TTS) model. 44.1 kHz
      native sample rate, 12 languages with auto-detection and
      code-switching. Voice cloning from 5–15 seconds of audio. Three
      transports: HTTP (sync), SSE (streaming), WebSocket (bidirectional
      streaming).
    - Lightning v3.1 Pro — premium TTS pool launched 2026-05-15. Same
      latency and concurrency as Lightning v3.1; a separate curated
      voice catalog of 39 voices across American, British, and Indian
      accents. Indian voices speak English + Hindi (code-switching);
      British and American voices speak English. No voice cloning. Pro
      is selected via `"model": "lightning_v3.1_pro"` body field on the
      unified `/waves/v1/tts` and `/waves/v1/tts/live` routes.
    - lightning-large — legacy TTS, used only for the older voice-cloning
      flow. Do not recommend for new integrations.
    - lightning-v2 — deprecated. Do not recommend.
    - Electron — current LLM (chat completions). OpenAI-compatible API
      at `POST /waves/v1/chat/completions` with `"model": "electron"`.
      <300 ms TTFT. 32K context window (combined input + output). 70
      languages with first-class Indic support (Hindi, Bengali, Tamil,
      Telugu, Marathi, Gujarati, Kannada, Malayalam, Punjabi, Odia, Urdu).
      Voice-agent-optimized: trained to emit a short filler phrase in
      the assistant message `content` field alongside `tool_calls` so
      downstream TTS can mask tool-call latency. Automatic prefix
      caching — `usage.prompt_tokens_details.cached_tokens` reports
      cached input; cached input billed at 75% off. Pricing: $0.40/M
      input, $0.10/M cached input, $1.60/M output. Limits: Standard
      plan 10 RPM / 3 concurrent; Enterprise 200 RPM / 20 concurrent.
      Drop-in replacement for OpenAI `chat/completions` — use the
      official OpenAI SDKs by changing `base_url` to
      `https://api.smallest.ai/waves/v1`.
    - Atoms — voice-agent platform built on top of Pulse + Lightning +
      Electron.

    ## Synonym handling — STT (Pulse)
    Map customer terminology to Pulse modes:
    - streaming / real-time / realtime / live / websocket / ws /
      continuous recognition / partial transcripts / interim results /
      universal-streaming / low-latency STT → Pulse WebSocket
      (wss://api.smallest.ai/waves/v1/pulse/get_text)
    - pre-recorded / prerecorded / batch / file transcription / upload /
      async / asynchronous / long-running / long-form / offline /
      fast transcription / submit-and-poll → Pulse pre-recorded HTTP
      (POST /waves/v1/pulse/get_text)
    Note: "asynchronous" is ambiguous across vendors. Google Cloud and
    AssemblyAI use it for batch; some others use it for streaming. If a
    user says "async transcription" without context, ask whether they
    mean file-based or live audio.

    ## Synonym handling — TTS (Lightning)
    - streaming / stream / websocket / ws / sse / server-sent events /
      chunked / real-time TTS / low-latency TTS / stream-input → unified
      `/waves/v1/tts/live` (HTTP POST returns SSE; WSS connect returns
      WebSocket — same URL, protocol-dispatched)
    - sync / synchronous / http / rest / convert / synthesize / generate /
      create speech / bytes / one-shot / blocking → unified
      `POST /waves/v1/tts`
    - The legacy model-named routes
      `/waves/v1/lightning-v3.1/get_speech`, `/stream`, and
      `/get_speech/stream` (WS) still work and continue to accept
      requests, but are marked deprecated. Recommend the unified routes
      to new users.
    - To pick the Pro pool: pass `"model": "lightning_v3.1_pro"` in the
      request body. Default is `"lightning_v3.1"`. Same body field works
      on both unified and legacy routes.

    ## Synonym handling — LLM (Electron)
    - LLM / language model / chat completions / chat / completion /
      generate text / GPT / Claude / Llama / drop-in OpenAI replacement /
      OpenAI-compatible / openai-compatible API → Electron
      (`POST /waves/v1/chat/completions` with `"model": "electron"`)
    - function calling / tool calling / tool use / function calls /
      agent / agentic → Electron tool calling
      (`tools` array in request, `tool_calls` in response)
    - prefix caching / prompt caching / cached tokens / context caching /
      kv cache → Electron automatic prefix caching (75% off cached input)
    - filler phrase / filler / thinking phrase / pre-tool message → the
      voice-agent-friendly behavior where Electron emits a short
      conversational `content` string alongside `tool_calls`
    - JSON mode / structured output → Electron supports
      `response_format: {"type": "json_object"}`
    - Indic / Indian languages / Hindi / Tamil / Bengali / Telugu /
      Marathi / Gujarati / Kannada / Malayalam / Punjabi / Odia / Urdu /
      Nepali / Sinhala → Electron (LLM) supports all of these natively
      with strong fluency

    ## Other voice-AI vocabulary
    - STT / ASR / automatic speech recognition / transcription /
      voice-to-text → Pulse
    - TTS / speech synthesis / voice synthesis / audio generation /
      neural voice / AI voice → Lightning v3.1
    - voice cloning / instant voice clone / professional voice clone /
      custom voice / voice model / speaker adaptation → Voice Cloning
      (POST /waves/v1/voice-cloning)
    - voice ID / voice_id / speaker_id / voice slug / voice name → use
      GET /waves/v1/{model}/get_voices to enumerate
    - speaker diarization / diarization / speaker labels / speaker
      separation / who-spoke-when → diarize=true
    - ITN / inverse text normalization / number formatting / smart format
      / formatted output → itn_normalize=true
    - TTFB / TTFA / first-byte latency / first-audio latency / TTFT /
      startup latency → see the Pulse model card (STT) or Lightning
      v3.1 model card (TTS) Performance & Benchmarks section
    - VAD / voice activity detection / endpointing / silence detection /
      turn detection / end-of-speech / EOS → eou_timeout_ms parameter
    - WER / word error rate / accuracy / transcription accuracy → see
      Pulse Performance page (FLEURS, ESB, WildASR benchmarks)

    ## Routing rules
    For latency, accuracy, throughput, or comparison questions, always
    cite the Performance / Benchmarks pages under each product. For
    voice cloning, point to the Voice Cloning section. Prefer Lightning
    v3.1 over older TTS models in every recommendation unless the user
    is migrating from a legacy voice clone.

    If the user has a question that the docs do not answer, or wants to
    dig deeper into edge cases, point them to the Smallest AI Discord:
    https://discord.gg/9WtSXv26WE

agents:
  page-description-source: description
  llms-txt: ./llms.txt
  page-directive: |
    This page is part of Smallest AI's developer documentation. When
    answering, prefer Lightning v3.1 (current TTS) and Pulse (current
    STT). Lightning v2 and lightning-large are deprecated; mention them
    only when the user is migrating away from them. Atoms is the
    voice-agent platform.

page-actions:
  options:
    copy-page: true
    view-as-markdown: true
    ask-ai: true
    chatgpt: true
    claude: true
    cursor: true
    claude-code: true

analytics:
  posthog:
    api-key: phc_sPVVNGLTV6b6CDMbmxkUjwNB6NvmEJKMcgT6EsN8m96j
    endpoint: https://us.i.posthog.com

css:
  - docs/assets/styles/global-styling.css

js:
  - docs/assets/scripts/analytics.js
  - docs/assets/scripts/commonroom-signals.js

layout:
  searchbar-placement: header
  page-width: full
  tabs-placement: header

redirects:
  # Short, shareable URLs for prospect-facing landing pages. Easier to
  # type, easier to remember, work in sales conversations.
  - source: /agents
    destination: /atoms
    permanent: false
  - source: /models
    destination: /waves/documentation/getting-started/models
    permanent: false

  # CLI page renamed across two rounds: agent -> agent-swarm -> agent-crew.
  # All historical URLs redirect to the current /agent-crew-cli.
  - source: /atoms/developer-guide/get-started/cli
    destination: /atoms/developer-guide/get-started/agent-crew-cli
    permanent: true
  - source: /atoms/developer-guide/get-started/agent-swarm
    destination: /atoms/developer-guide/get-started/agent-crew-cli
    permanent: true
  - source: /atoms/developer-guide/get-started/agent-swarm-cli
    destination: /atoms/developer-guide/get-started/agent-crew-cli
    permanent: true
  # Browser SDK was renamed WebSocket SDK; the section also moved from
  # the Developer Guide tab to Atoms Platform.
  - source: /atoms/developer-guide/integrate/browser-sdk
    destination: /atoms/atoms-platform/integrate/web-socket-sdk
    permanent: true
  - source: /atoms/developer-guide/integrate/websocket-sdk
    destination: /atoms/atoms-platform/integrate/web-socket-sdk
    permanent: true
  - source: /atoms/developer-guide/integrate/web-socket-sdk
    destination: /atoms/atoms-platform/integrate/web-socket-sdk
    permanent: true
  - source: /atoms/developer-guide/integrate/embed-a-voice-agent
    destination: /atoms/atoms-platform/integrate/embed-a-voice-agent
    permanent: true
  - source: /atoms/developer-guide/integrate/mobile/react-native
    destination: /atoms/atoms-platform/integrate/mobile-integrations/react-native
    permanent: true
  - source: /atoms/developer-guide/integrate/mobile/ios-swift
    destination: /atoms/atoms-platform/integrate/mobile-integrations/i-os-swift
    permanent: true
  - source: /atoms/developer-guide/integrate/mobile/android-kotlin
    destination: /atoms/atoms-platform/integrate/mobile-integrations/android-kotlin
    permanent: true
  - source: /atoms/developer-guide/integrate/mobile/flutter
    destination: /atoms/atoms-platform/integrate/mobile-integrations/flutter
    permanent: true
  # Legacy Pulse REST API-ref page consolidated into the unified /transcribe
  # page (PR #187). The endpoint /waves/v1/pulse/get_text continues to work
  # at the API level; the dedicated docs page is gone to avoid duplication.
  - source: /waves/api-reference/api-reference/speech-to-text/pulse
    destination: /waves/api-reference/api-reference/speech-to-text/transcribe
    permanent: true
  - source: /waves/v-4-0-0/api-reference/api-reference/speech-to-text/pulse
    destination: /waves/v-4-0-0/api-reference/api-reference/speech-to-text/transcribe
    permanent: true
  - source: /waves/api-reference/api-reference/speech-to-text/pulse-speech-to-text
    destination: /waves/api-reference/api-reference/speech-to-text/transcribe
    permanent: true
  - source: /waves/v-4-0-0/api-reference/api-reference/speech-to-text/pulse-speech-to-text
    destination: /waves/v-4-0-0/api-reference/api-reference/speech-to-text/transcribe
    permanent: true

  # Pulse STT numeric-formatting feature page removed in favor of itn_normalize.
  # ITN supersedes the old `numerals` parameter and covers more entities.
  - source: /waves/documentation/speech-to-text-pulse/features/numeric-formatting
    destination: /waves/documentation/speech-to-text-pulse/features/inverse-text-normalization
    permanent: true
  - source: /waves/v-4-0-0/documentation/speech-to-text-pulse/features/numeric-formatting
    destination: /waves/documentation/speech-to-text-pulse/features/inverse-text-normalization
    permanent: true

  # Atoms API reference endpoints renamed / moved between sections.
  - source: /atoms/api-reference/api-reference/agents/update-an-agent
    destination: /atoms/api-reference/api-reference/agents/update-agent-metadata
    permanent: true
  - source: /atoms/api-reference/api-reference/workflows/update-workflow-configuration
    destination: /atoms/api-reference/api-reference/agents/update-workflow-configuration
    permanent: true
  - source: /atoms/api-reference/api-reference/calls/subscribe-to-live-call-events-sse
    destination: /atoms/api-reference/api-reference/live-transcripts/subscribe-to-live-call-events-sse
    permanent: true

  # Waves integrations moved from /waves/documentation/integrations/* to
  # /waves/integrations/*. Some slugs also normalised (camelCase -> kebab-case).
  - source: /waves/documentation/integrations/livekit
    destination: /waves/integrations/live-kit
    permanent: true
  - source: /waves/documentation/integrations/live-kit
    destination: /waves/integrations/live-kit
    permanent: true
  - source: /waves/documentation/integrations/openwhispr
    destination: /waves/integrations/open-whispr
    permanent: true
  - source: /waves/documentation/integrations/open-whispr
    destination: /waves/integrations/open-whispr
    permanent: true
  - source: /waves/documentation/integrations/:slug
    destination: /waves/integrations/:slug
    permanent: true

  # Removed waves pages -> nearest live page.
  - source: /waves/documentation/voice-cloning/types-of-clone
    destination: /waves/documentation/voice-cloning/instant-clone-ui
    permanent: true
  - source: /waves/documentation/speech-to-text-pulse/features/full-transcript
    destination: /waves/documentation/speech-to-text-pulse/features/word-timestamps
    permanent: true

  # Sample / placeholder URLs from in-page code that ended up indexed.
  - source: /agent/from-template
    destination: /atoms/atoms-platform/single-prompt-agents/creating-your-agent/from-template
    permanent: true
  - source: /agent/template
    destination: /atoms/atoms-platform/single-prompt-agents/creating-your-agent/from-template
    permanent: true
  - source: /agent/:rest
    destination: /atoms/atoms-platform/get-started/quick-start
    permanent: true
  - source: /agent/:agentId/:rest
    destination: /atoms/atoms-platform/get-started/quick-start
    permanent: true
  - source: /agent/:agentId/drafts/:draftId
    destination: /atoms/atoms-platform/get-started/quick-start
    permanent: true
  - source: /agent/:agentId/drafts/:draftId/:rest
    destination: /atoms/atoms-platform/get-started/quick-start
    permanent: true

  # Atoms console-shaped paths that ended up indexed.
  - source: /audience
    destination: /atoms/atoms-platform/deployment/audiences
    permanent: true
  - source: /audience/:rest
    destination: /atoms/atoms-platform/deployment/audiences
    permanent: true
  - source: /audience/:audienceId/:rest
    destination: /atoms/atoms-platform/deployment/audiences
    permanent: true
  - source: /campaign
    destination: /atoms/atoms-platform/deployment/campaigns
    permanent: true
  - source: /campaign/:rest
    destination: /atoms/atoms-platform/deployment/campaigns
    permanent: true
  - source: /campaign/:campaignId/:rest
    destination: /atoms/atoms-platform/deployment/campaigns
    permanent: true
  - source: /conversation
    destination: /atoms/atoms-platform/analytics-logs/overview
    permanent: true
  - source: /knowledgebase/:rest
    destination: /atoms/atoms-platform/features/knowledge-base
    permanent: true
  - source: /knowledgebase/:id/:rest
    destination: /atoms/atoms-platform/features/knowledge-base
    permanent: true
  - source: /knowledgebase/:id/items/:itemId
    destination: /atoms/atoms-platform/features/knowledge-base
    permanent: true
  - source: /webhook
    destination: /atoms/atoms-platform/features/webhooks
    permanent: true
  - source: /webhook/:rest
    destination: /atoms/atoms-platform/features/webhooks
    permanent: true
  - source: /workflow
    destination: /atoms/atoms-platform/conversational-flow-agents/overview
    permanent: true
  - source: /product/import-phone-number
    destination: /atoms/atoms-platform/deployment/phone-numbers
    permanent: true

  # Atoms compliance API paths.
  - source: /compliance/applications
    destination: /atoms/api-reference/api-reference/compliance/get-compliance-status
    permanent: true
  - source: /compliance/requirements
    destination: /atoms/api-reference/api-reference/compliance/get-compliance-requirements
    permanent: true

  # Bare API endpoint paths that got indexed from code samples.
  - source: /chat/completions
    destination: /atoms/api-reference/api-reference
    permanent: true
  - source: /api/speak
    destination: /waves/documentation/text-to-speech-lightning/quickstart
    permanent: true
  - source: /api/get-stream-url
    destination: /waves/documentation/text-to-speech-lightning/quickstart
    permanent: true
  - source: /api/transcribe-stream
    destination: /waves/documentation/speech-to-text-pulse/quickstart
    permanent: true
  - source: /api/transcribe-mic-stream
    destination: /waves/documentation/speech-to-text-pulse/quickstart
    permanent: true
  - source: /v1/listen
    destination: /waves/documentation/speech-to-text-pulse/quickstart
    permanent: true
  - source: /v1/listen/stream
    destination: /waves/documentation/speech-to-text-pulse/quickstart
    permanent: true
  - source: /v1/speak
    destination: /waves/documentation/text-to-speech-lightning/quickstart
    permanent: true
  - source: /lightning-large/add_voice
    destination: /waves/documentation/voice-cloning/instant-clone-ui
    permanent: true
  - source: /lightning-large/:rest
    destination: /waves/documentation/text-to-speech-lightning/quickstart
    permanent: true
  - source: /waves/v1/lightning-large
    destination: /waves/documentation/text-to-speech-lightning/quickstart
    permanent: true
  - source: /waves/v1/lightning-large/:rest
    destination: /waves/documentation/text-to-speech-lightning/quickstart
    permanent: true
  - source: /waves/v1/lightning-v2/get_speech
    destination: /waves/documentation/text-to-speech-lightning/quickstart
    permanent: true
  - source: /waves/v1/lightning-v2/get_speech/stream
    destination: /waves/documentation/text-to-speech-lightning/quickstart
    permanent: true
  - source: /waves/v1/lightning-v3.1/get_speech/stream
    destination: /waves/documentation/text-to-speech-lightning/quickstart
    permanent: true
  - source: /app/models
    destination: /waves/documentation/getting-started/introduction
    permanent: true
  - source: /app/models/:rest
    destination: /waves/documentation/getting-started/introduction
    permanent: true
  - source: /transcribe
    destination: /waves/documentation/speech-to-text-pulse/quickstart
    permanent: true
  - source: /stream
    destination: /waves/documentation/text-to-speech-lightning/quickstart
    permanent: true
  - source: /start
    destination: /atoms/atoms-platform/get-started/quick-start
    permanent: true
  # API reference reorganization (2026-05-22): flattened Voices, Voice Cloning,
  # and Pronunciation Dictionaries directly under the Text to Speech section
  # (no subsection dropdowns). Old top-level slugs (HEAD-verified live)
  # redirect to their new flat TTS counterparts. Slugs derive from each
  # operation's operationId in the base waves spec.
  - source: /waves/api-reference/api-reference/voices/get-waves-voices
    destination: /waves/api-reference/api-reference/text-to-speech/get-waves-voices
    permanent: true
  - source: /waves/api-reference/api-reference/voice-cloning/create-voice-clone
    destination: /waves/api-reference/api-reference/text-to-speech/create-voice-clone
    permanent: true
  - source: /waves/api-reference/api-reference/voice-cloning/list-voice-clones
    destination: /waves/api-reference/api-reference/text-to-speech/list-voice-clones
    permanent: true
  - source: /waves/api-reference/api-reference/voice-cloning/delete-voice-clone
    destination: /waves/api-reference/api-reference/text-to-speech/delete-voice-clone
    permanent: true
  - source: /waves/api-reference/api-reference/voice-cloning/add-voice-to-model
    destination: /waves/api-reference/api-reference/text-to-speech/add-voice-to-model
    permanent: true
  - source: /waves/api-reference/api-reference/voice-cloning/get-cloned-voices
    destination: /waves/api-reference/api-reference/text-to-speech/get-cloned-voices
    permanent: true
  - source: /waves/api-reference/api-reference/pronunciation-dictionaries/get-pronunciation-dicts
    destination: /waves/api-reference/api-reference/text-to-speech/get-pronunciation-dicts
    permanent: true
  - source: /waves/api-reference/api-reference/pronunciation-dictionaries/create-pronunciation-dict
    destination: /waves/api-reference/api-reference/text-to-speech/create-pronunciation-dict
    permanent: true
  - source: /waves/api-reference/api-reference/pronunciation-dictionaries/update-pronunciation-dict
    destination: /waves/api-reference/api-reference/text-to-speech/update-pronunciation-dict
    permanent: true
  - source: /waves/api-reference/api-reference/pronunciation-dictionaries/delete-pronunciation-dict
    destination: /waves/api-reference/api-reference/text-to-speech/delete-pronunciation-dict
    permanent: true

  # Atoms Developer Guide — "Agent Crew Core Concepts" subsection rename.
  # Earlier slug was derived from the `core-concepts/` directory; Fern now
  # derives it from the section title "Agent Crew Core Concepts". The old
  # URLs were shared with customers in onboarding and other channels, so
  # we redirect them to the current slugs to keep external links working.
  - source: /atoms/developer-guide/get-started/core-concepts/overview
    destination: /atoms/developer-guide/get-started/agent-crew-core-concepts/overview
    permanent: true
  - source: /atoms/developer-guide/get-started/core-concepts/nodes
    destination: /atoms/developer-guide/get-started/agent-crew-core-concepts/nodes
    permanent: true
  - source: /atoms/developer-guide/get-started/core-concepts/events
    destination: /atoms/developer-guide/get-started/agent-crew-core-concepts/events
    permanent: true
  - source: /atoms/developer-guide/get-started/core-concepts/graphs
    destination: /atoms/developer-guide/get-started/agent-crew-core-concepts/graphs
    permanent: true
  - source: /atoms/developer-guide/get-started/core-concepts/sessions
    destination: /atoms/developer-guide/get-started/agent-crew-core-concepts/sessions
    permanent: true

  # Atoms Developer Guide — "Error Handling" page slug shortened. The
  # earlier `agent-crew-error-handling` was redundant inside the
  # Agent Crew section. Redirect preserves customer-shared links.
  - source: /atoms/developer-guide/get-started/agent-crew-error-handling
    destination: /atoms/developer-guide/get-started/error-handling
    permanent: true

  # Atoms Developer Guide — "Agents" section renamed to "Agent Crews" to
  # match the SDK surface (Crew CLI, CrewSession, etc.). Redirects cover
  # the overview + every sub-page so customer-shared links keep working.
  - source: /atoms/developer-guide/build/agents/overview
    destination: /atoms/developer-guide/build/agent-crews/overview
    permanent: true
  - source: /atoms/developer-guide/build/agents/llm/overview
    destination: /atoms/developer-guide/build/agent-crews/llm/overview
    permanent: true
  - source: /atoms/developer-guide/build/agents/llm/prompts
    destination: /atoms/developer-guide/build/agent-crews/llm/prompts
    permanent: true
  - source: /atoms/developer-guide/build/agents/llm/byom
    destination: /atoms/developer-guide/build/agent-crews/llm/byom
    permanent: true
  - source: /atoms/developer-guide/build/agents/llm/llm-settings
    destination: /atoms/developer-guide/build/agent-crews/llm/llm-settings
    permanent: true
  - source: /atoms/developer-guide/build/agents/tools/overview
    destination: /atoms/developer-guide/build/agent-crews/tools/overview
    permanent: true
  - source: /atoms/developer-guide/build/agents/tools/defining-tools
    destination: /atoms/developer-guide/build/agent-crews/tools/defining-tools
    permanent: true
  - source: /atoms/developer-guide/build/agents/tools/built-in-tools
    destination: /atoms/developer-guide/build/agent-crews/tools/built-in-tools
    permanent: true
  - source: /atoms/developer-guide/build/agents/tools/executing-tools
    destination: /atoms/developer-guide/build/agent-crews/tools/executing-tools
    permanent: true
  - source: /atoms/developer-guide/build/agents/patterns/conversation-flow-design
    destination: /atoms/developer-guide/build/agent-crews/patterns/conversation-flow-design
    permanent: true

  # Waves LLM (Electron) page-slug corrections. The page titles were
  # adjusted at launch ("Tool Calling" -> "Tool / Function Calling",
  # "Migrate from OpenAI" -> "Migrate from Open AI") which changed the
  # Fern-derived slugs. Customer-shared links from launch use the older.
  - source: /waves/documentation/llm-electron/tool-calling
    destination: /waves/documentation/llm-electron/tool-function-calling
    permanent: true
  - source: /waves/documentation/llm-electron/migrate-from-openai
    destination: /waves/documentation/llm-electron/migrate-from-open-ai
    permanent: true

  # Waves Lightning streaming page renamed from `stream-tts` to
  # `streaming` to align with the section heading.
  - source: /waves/documentation/text-to-speech-lightning/stream-tts
    destination: /waves/documentation/text-to-speech-lightning/streaming
    permanent: true

  # Waves Pulse STT — age detection was removed; the remaining feature
  # renamed from "Age and Gender Detection" to just "Gender Detection".
  - source: /waves/documentation/speech-to-text-pulse/features/age-and-gender-detection
    destination: /waves/documentation/speech-to-text-pulse/features/gender-detection
    permanent: true

  # Waves TTS API ref slugs. The unified `/waves/v1/tts` family rendered
  # with different Fern-emitted slugs than the original
  # `live-tts-web-socket` / `stream-speech-sse` guesses in the spec.
  - source: /waves/api-reference/api-reference/text-to-speech/live-tts-web-socket
    destination: /waves/api-reference/api-reference/text-to-speech/tts
    permanent: true
  - source: /waves/api-reference/api-reference/text-to-speech/stream-speech-sse
    destination: /waves/api-reference/api-reference/text-to-speech/synthesize-speech-sse
    permanent: true

  # Waves voices/languages page consolidated and moved out of the
  # versioned `/v-4-0-0/` segment.
  - source: /waves/v-4-0-0/documentation/text-to-speech/get-voice-models-langs
    destination: /waves/documentation/text-to-speech-lightning/voices-languages
    permanent: true

products:
  - display-name: Voice Agents
    path: ./products/atoms.yml
    icon: fa-solid fa-robot
    slug: atoms
  - display-name: Models
    path: ./products/waves/versions/v4.0.0.yml
    icon: fa-solid fa-waveform-lines
    slug: waves
    versions:
      - display-name: v4.0.0
        path: ./products/waves/versions/v4.0.0.yml
      - display-name: v3.0.1
        path: ./products/waves/versions/v3.0.1.yml
      - display-name: v2.2.0
        path: ./products/waves/versions/v2.2.0.yml

colors:
  accentPrimary:
    dark: '#2A9D8F'
    light: '#083b4d'
  background:
    dark: '#000000'
    light: '#FFFFFF'

theme:
  page-actions: toolbar
  footer-nav: minimal
  product-switcher: toggle


logo:
  dark: docs/assets/logo_light.png
  light: docs/assets/logo_dark.png
  height: 51

favicon: docs/assets/favicon.svg

default-language: python
